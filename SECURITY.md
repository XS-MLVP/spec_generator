
# Security Policy

本项目主要处理公开硬件源码和本地生成文档。仓库根目录的输入、产物、本地配置及 `.ucagent/`（含工具凭据密钥）已被忽略；独立工作区和自定义路径需自行设置忽略规则，已跟踪文件仍会被 Git 记录。`.gitignore` 不是安全边界。请不要在产物、日志、参考资料或 Git 历史中保存或提交：

- API token、SSH private key、密码或 cookie。
- 内部仓库凭据和未公开下载地址。
- 含用户名、访问令牌或私有目录结构的本机绝对路径。
- 未授权公开的设计资料。

引用外部材料时，应确认其许可和再分发条件，并按 [维护指南](CONTRIBUTING.md) 记录来源与用途。模块产物需要长期保存时，应使用具备适当访问控制的项目外制品存储。

发现凭据泄露时，应立即撤销凭据并联系仓库维护者处理历史清理，不要仅通过后续 commit 删除文件。

安全问题请通过 GitHub 仓库的私密安全报告渠道提交；不要在公开 Issue 中披露有效凭据或未修复漏洞细节。
